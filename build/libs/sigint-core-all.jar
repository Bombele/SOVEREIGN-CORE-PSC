#!/bin/bash
# Création des dossiers critiques
mkdir -p build/libs core/sigint data/audit data/keys scripts vectors/financial

# Installation des outils système manquants (Correction pour l'erreur 'nc' et 'python')
sudo apt-get update && sudo apt-get install -y netcat-openbsd python3-pip
pip3 install netfilterqueue scapy pymodbus redis zmq

# Attribution des permissions
chmod +x scripts/*.sh

echo "✅ Dossiers créés et dépendances installées."
